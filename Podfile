# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-webrtc-test' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-webrtc-test

  platform :ios, '9.0'
  pod 'GoogleWebRTC'
  pod 'Starscream', '~> 3.0'
  pod 'SwiftyJSON', '~> 4.0'

end
